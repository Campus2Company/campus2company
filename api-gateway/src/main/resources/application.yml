server:
  port: 8080

spring:
  application:
    name: api-gateway

# JWT Configuration (validation only — same secret as auth-service)
jwt:
  secret: ${JWT_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: health,info
